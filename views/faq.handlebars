<div class="container">
	<div class="row">
		<div class="col-md-12">
			<h1 class="text-center">FAQ</h1>
	<div id="faq">
		<ul>
	{{#each faqtable}}
        <strong>{{this.questions}}</strong>
        <br>
		{{this.answers}}
        <br><br>
	{{/each}}
		</ul>
	</div>
		</div>
	</div>
</div>